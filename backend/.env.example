# Anthropic API key for vision and LLM calls
ANTHROPIC_API_KEY=

# Database connection (Phase 1+)
DATABASE_URL=postgresql://cooksense:cooksense@localhost:5432/cooksense

# ChromaDB connection (Phase 1+)
# Leave CHROMA_HOST empty to use embedded persistent client (dev default).
# Set host + token to use ChromaDB Cloud (prod).
CHROMA_HOST=
CHROMA_API_KEY=
CHROMA_PERSIST_DIR=./data/.chroma

# Translation cache directory (Phase 1+)
TRANSLATION_CACHE_DIR=./data/translations

# Embedding model used by recipe repository (multilingual)
EMBEDDING_MODEL=sentence-transformers/distiluse-base-multilingual-cased-v2

# CORS — adjust for production
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Logging
LOG_LEVEL=INFO

# --- Phase 3: meal planning + shopping list ---
# Anthropic models
ANTHROPIC_MODEL_PLANNING=claude-sonnet-4-6
ANTHROPIC_MODEL_SHOPPING=claude-haiku-4-5
ANTHROPIC_MAX_TOKENS_PLANNING=4096
ANTHROPIC_MAX_TOKENS_SHOPPING=1024

# Meal plan structure
MEAL_PLAN_DEFAULT_DAYS=3
MEAL_PLAN_MEALS_PER_DAY=breakfast,lunch,dinner
MEAL_PLAN_CANDIDATE_POOL_SIZE=50

# Daily rate limit (planning is the most expensive op)
RATE_LIMIT_MEAL_PLAN_PER_DAY=1

# Cached meal plan TTL in seconds (7 days)
CACHE_TTL_MEAL_PLAN_SECONDS=604800
